[ctrl+insert = copy
 shift+insert = paste]

# Open app folder -> right click -> git bash 

[(incase not available)
*open git bash from windows -> cd "copy path of folder"
]

# git config --global user.name <name>
[(change to full name)
*git config --replace-all user.name <"full name">
] = git config user.name [to see status]

# git config --global user.email <email> 
= git config --global user.email [to see status]

# touch .gitignore [if gitignore is ungitavailavle]

# git init

# git add .

# git commit -m "Initial commit"
 
# git status [just to see]

# git remote add origin <SSH-link>

# git push origin (branch_name)

<-- After push add new items -->

# git status [just to see]

# git add .

# git commit -m "Added <item_name>"

# git push origin (branch_name)